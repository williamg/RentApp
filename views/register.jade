extends simpleLayout

block content
  #wrapper
    #form-center
      #content
          h2 Register for ShareIt!
          form
            input(placeholder="John Doe")#name
            input(placeholder="jdoe@andrew.cmu.edu")#email
            input(placeholder="password", type="password")#password
            input(placeholder="password", type="password")#confirm
            input(type="file", value="Choose a profile picture!")#profpic
            input(type="submit")#submit Register

  script(src="/js/register.js")
